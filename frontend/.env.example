# Relativ base-URL — la denne stå som /api/v1.
# Vite-dev-serveren proxy-er alle /api-kall server-side til FastAPI,
# så nettleseren trenger aldri å vite om port 8000.
# Absolutt URL (http://localhost:8000/api/v1) bryter LAN-testing.
VITE_API_BASE_URL=/api/v1
